# Public base URL of the API server consumed by the browser.
# Required in production. Must be the fully-qualified URL of the deployed api-server
# (e.g. https://api.sgnl.example.com). No trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
